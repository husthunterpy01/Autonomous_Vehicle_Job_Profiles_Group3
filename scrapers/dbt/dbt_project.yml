name: av_jobs
version: "1.0.0"
config-version: 2
profile: av_jobs

model-paths: ["models"]
macro-paths: ["macros"]
target-path: target
clean-targets: ["target", "dbt_packages"]

models:
  av_jobs:
    +materialized: table
    +schema: bronze
