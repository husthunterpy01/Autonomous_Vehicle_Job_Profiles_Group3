# Environment / secrets
.env
.env.local
.env.*.local
!.env.sample
!.env.example

# Python virtualenvs
.venv/
venv/
env/

# Python bytecode / packaging
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
*.egg

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
.nox/

# Logs
*.log
logs/

# Generated scraper/pipeline output, trained model artifacts, DB backups (BE-1)
/data/

# In-progress diagnostic logs from one-off backend scripts (resumable,
# regenerated on every run - not a finished audit trail like a completed
# backfill's own committed log)
backend/scripts/llm_confidence_log_all_valid_jobs.jsonl

# SetFit/HF Trainer checkpoints (scratch state, not the final saved model) -
# unanchored since these can land at repo root or under notebooks/ depending
# on the training tool's cwd
checkpoints/

# Legacy per-company scrapers (superseded by strategy + scraper_main)
scrapers/base_scraper.py
scrapers/*_scraper.py

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Node / Next.js (frontend)
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Temporary unpacked template folder
.tmp_jobhunter/

# Reasonix tool workspace data
.reasonix/

# Generated JSONL logs (pipeline output, diagnostic dumps)
*.jsonl

# Training-curve source numbers; the PNG snapshot is kept in git
scrapers/data/snapshots/train_test_accuracy_by_iteration.json
